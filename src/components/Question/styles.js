import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  questionView: {
    flex: 1,
    padding: 20,
    alignItems: 'flex-start'
  },
  questionText: {
    marginBottom: 10,
  },
});
